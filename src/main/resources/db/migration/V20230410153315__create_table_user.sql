use `audio`;

CREATE TABLE `user` (
                        `id` INT(4) NOT NULL AUTO_INCREMENT PRIMARY KEY ,
                        `name` VARCHAR(50) NOT NULL ,
                        `pwd` VARCHAR(100) NOT NULL
);
